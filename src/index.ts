/**
 * Docking Layout plugin, Host half. The empty apply exposes the package to a
 * Cordis composition; all behavior lives in the browser client entry.
 */

/** Host plugin body — no Host routes, listeners, or processes. */
export function apply(): void {}
